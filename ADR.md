# ADR.md

This repository uses `DECISIONS.md` as the primary decision log instead of separate per-decision ADR files.

If we ever need formal ADRs later, they should mirror the decisions already recorded there.
