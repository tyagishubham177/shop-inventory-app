# Backend Instructions

- Protect every non-public route.
- Validate inputs before database work.
- Keep responses predictable and typed.
- Match implementation to `docs/api-contracts.md` and `docs/design.md`.
